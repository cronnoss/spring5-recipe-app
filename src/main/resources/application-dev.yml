spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cro_dev
    username: cro_dev_user
    password: cro
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
#    properties:
#      javax:
#        persistence:
#         schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: cro_database_create.sql
